<main role="main" class="container-fluid flex-fill" id="settings-container">
  {% if logged_in == 'false' %}

    <div class="container" id="not-logged-in">
      <div class="row align-self-center">
        <div class="col-12 align-self-center">
          <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading">There was an issue...</h4>
            <p class="mb-0">Before you can use this feature of the portal, you need to login to your account. Please use the navigation menu above to login.</p>
          </div>
        </div>
      </div>
    </div>
  {% else %}

    <div class="container" id="settings">
      <div class="row">
        <div class="col-12">
          <h2>Email Settings</h2>
          <hr>
          <p>Add your email to reply to Tickets from within the portal, so you can send immediate responses to the Customer Success Team.</p>
          <div class="email-settings-form">
            <form action="/settings" method="post" id="email-settings-form">
              <div class="form-group mb-4">
                <input type="text" class="form-control" autocomplete="new-password" value="{{ email }}" placeholder="Email*" name="emailAddress" required />
              </div>
              <div class="form-group mb-4">
                <input type="password" class="form-control" auto-complete="new-password" placeholder="{% if placeholder is not empty %}{{ placeholder }}{% else %}Password*{% endif %}" name="emailPassword" required />
              </div>
              <div class="form-row mb-0">
                <div class="form-group col-xs-12 col-sm-10 col-md-10 col-lg-11">
                  <input type="text" class="form-control" value="{{ server }}" placeholder="Outgoing Server*" name="outgoingServer" id="outgoingServer" required />
                </div>
                <div class="form-group col-xs-12 col-sm-2 col-md-2 col-lg-1">
                  <input type="number" class="form-control" value="{{ port }}" placeholder="Port*" name="outgoingPort" id="outgoingPort" required />
                </div>
              </div>
              <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" id="enableSSL" name="enableSSL" {% if ssl == true %}checked{% endif %}>
                <label class="form-check-label" for="gridCheck">
                  Enable SSL
                </label>
              </div>
              <button type="submit" class="btn btn-lg btn-primary mt-2 save">Save</button>
            </form>
          </div>
        </div>
      </div>
    </div>

  {% endif %}
</main>
